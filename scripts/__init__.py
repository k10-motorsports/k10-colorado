"""Prodrive AC Builder — pipeline package.

Headless macOS pipeline that turns a real-world road loop into an Assetto Corsa track.
See CLAUDE.md for conventions (units = meters, Y-up, surface naming, source priority) and
docs/PLAN.md for the full plan. Every track is reproducible from its ``track.config.json``.
"""

__version__ = "0.1.0"
